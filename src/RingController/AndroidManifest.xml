<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application android:allowBackup="true" android:icon="@mipmap/appicon" android:label="@string/app_name" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true">
  </application>
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <!-- For changing system brightness from an AccessibilityService -->
  <uses-permission android:name="android.permission.WRITE_SETTINGS" />
  <!-- Some devices require this to dispatch media transport key events -->
  <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />
  <!-- Flashlight toggle (CameraManager#setTorchMode) -->
  <uses-permission android:name="android.permission.CAMERA" />
  <!-- GitHub APK sideload: enumerate launchable apps (Play Store QUERY_ALL_PACKAGES rules do not apply) -->
  <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
</manifest>